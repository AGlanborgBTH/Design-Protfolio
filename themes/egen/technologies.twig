{% include 'incl/header.twig' %}

<body>
	<div class="centering">
		<div class="restraint">
			{% include 'incl/nav.twig' %}

			<div class="main" role="main">
				<div class="main-width grid">
					{{ content }}

					{% for page in pages("technology") %}
						<div>
							<a href="{{ page.url}}">
								{{ page.title }}
							</a>
						</div>
					{% endfor %}
                    
				</div>
			</div>

			{% include 'incl/footer.twig' %}
		</div>
	</div>
</body>
